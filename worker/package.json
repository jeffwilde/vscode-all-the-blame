{
	"name": "vscode-all-the-blame-preview-worker",
	"version": "0.0.0",
	"private": true,
	"description": "Cloudflare Worker that routes /pr/<n>/ requests to per-PR builds in R2, resolved via KV pointers.",
	"scripts": {
		"build": "esbuild src/index.ts --bundle --format=esm --target=es2022 --platform=neutral --outfile=dist/index.js",
		"typecheck": "tsc --noEmit"
	},
	"devDependencies": {
		"@cloudflare/workers-types": "^4.20260401.0",
		"esbuild": "^0.25.0",
		"typescript": "^5.9.0"
	}
}
